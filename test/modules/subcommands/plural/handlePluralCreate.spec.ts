import { assert } from "chai";

import { handlePluralCreate } from "../../../../src/modules/subcommands/plural/handlePluralCreate";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handlePluralCreate);
  });
});
