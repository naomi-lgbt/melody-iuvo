import { assert } from "chai";

import { handlePluralFront } from "../../../../src/modules/subcommands/plural/handlePluralFront";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handlePluralFront);
  });
});
