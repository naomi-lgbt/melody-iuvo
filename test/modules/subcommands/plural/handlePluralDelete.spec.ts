import { assert } from "chai";

import { handlePluralDelete } from "../../../../src/modules/subcommands/plural/handlePluralDelete";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handlePluralDelete);
  });
});
