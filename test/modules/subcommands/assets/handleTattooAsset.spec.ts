import { assert } from "chai";

import { handleTattooAsset } from "../../../../src/modules/subcommands/assets/handleTattooAsset";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleTattooAsset);
  });
});
