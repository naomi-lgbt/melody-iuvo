import { assert } from "chai";

import { handlePortraitAsset } from "../../../../src/modules/subcommands/assets/handlePortraitAsset";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handlePortraitAsset);
  });
});
