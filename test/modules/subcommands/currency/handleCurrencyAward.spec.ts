import { assert } from "chai";

import { handleCurrencyAward } from "../../../../src/modules/subcommands/currency/handleCurrencyAward";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleCurrencyAward);
  });
});
