import { assert } from "chai";

import { handleCurrencyCache } from "../../../../src/modules/subcommands/currency/handleCurrencyCache";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleCurrencyCache);
  });
});
