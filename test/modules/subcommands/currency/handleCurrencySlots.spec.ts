import { assert } from "chai";

import { handleCurrencySlots } from "../../../../src/modules/subcommands/currency/handleCurrencySlots";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleCurrencySlots);
  });
});
