import { assert } from "chai";

import { handleCurrencyAbout } from "../../../../src/modules/subcommands/currency/handleCurrencyAbout";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleCurrencyAbout);
  });
});
