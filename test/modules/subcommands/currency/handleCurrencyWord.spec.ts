import { assert } from "chai";

import { handleCurrencyWord } from "../../../../src/modules/subcommands/currency/handleCurrencyWord";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleCurrencyWord);
  });
});
