import { assert } from "chai";

import { handleNaomiAsk } from "../../../../src/modules/subcommands/naomi/handleNaomiAsk";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleNaomiAsk);
  });
});
