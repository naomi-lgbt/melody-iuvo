import { assert } from "chai";

import { handleNaomiSays } from "../../../../src/modules/subcommands/naomi/handleNaomiSays";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleNaomiSays);
  });
});
