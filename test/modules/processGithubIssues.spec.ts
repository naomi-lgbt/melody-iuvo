import { assert } from "chai";

import { processGithubIssues } from "../../src/modules/processGithubIssues";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(processGithubIssues);
  });
});
