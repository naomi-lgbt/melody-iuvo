import { assert } from "chai";

import { ticketOpenHandler } from "../../../src/modules/buttons/ticketOpen";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(ticketOpenHandler);
  });
});
