import { assert } from "chai";

import { ticketClaimHandler } from "../../../src/modules/buttons/ticketClaim";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(ticketClaimHandler);
  });
});
