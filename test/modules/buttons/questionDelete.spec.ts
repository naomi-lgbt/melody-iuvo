import { assert } from "chai";

import { questionDelete } from "../../../src/modules/buttons/questionDelete";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(questionDelete);
  });
});
