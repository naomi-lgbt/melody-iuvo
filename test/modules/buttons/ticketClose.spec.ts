import { assert } from "chai";

import { ticketCloseHandler } from "../../../src/modules/buttons/ticketClose";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(ticketCloseHandler);
  });
});
