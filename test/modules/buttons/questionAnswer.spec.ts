import { assert } from "chai";

import { questionAnswer } from "../../../src/modules/buttons/questionAnswer";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(questionAnswer);
  });
});
