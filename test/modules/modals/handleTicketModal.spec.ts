import { assert } from "chai";

import { handleTicketModal } from "../../../src/modules/modals/handleTicketModal";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(handleTicketModal);
  });
});
