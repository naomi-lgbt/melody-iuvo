import { assert } from "chai";

import { processAnswerModal } from "../../../src/modules/modals/processAnswerModal";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(processAnswerModal);
  });
});
