import { assert } from "chai";

import { processWordGuess } from "../../../src/modules/modals/processWordGuess";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(processWordGuess);
  });
});
