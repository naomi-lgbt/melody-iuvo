import { assert } from "chai";

import { processOnboardingModal } from "../../../src/modules/modals/processOnboardingModal";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(processOnboardingModal);
  });
});
