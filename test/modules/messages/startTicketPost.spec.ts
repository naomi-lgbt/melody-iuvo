import { assert } from "chai";

import { startTicketPost } from "../../../src/modules/messages/startTicketPost";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(startTicketPost);
  });
});
