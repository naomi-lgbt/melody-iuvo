import { assert } from "chai";

import { pruneInactiveUsers } from "../../../src/modules/messages/pruneInactiveUsers";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(pruneInactiveUsers);
  });
});
