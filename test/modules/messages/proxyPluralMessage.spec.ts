import { assert } from "chai";

import { proxyPluralMessage } from "../../../src/modules/messages/proxyPluralMessage";

suite("This is an example test", () => {
  test("It uses the assert API", () => {
    assert.isDefined(proxyPluralMessage);
  });
});
